# Secrets
.env

# Personal config and data — every user's own job search
cv.md
portals.yml
data/*
# Tracked exception: dedup state for the Discord-notify cloud routine, so a
# fresh cloud checkout doesn't re-notify about postings already seen locally.
!data/scan-history.tsv
# Tracked exception: which scan-history.tsv URLs the Discord routine has
# already posted, so it only notifies about genuinely new postings.
!data/discord-notified.tsv
batch/
reports/
output/
jds/
interview-prep/
article-digest.md

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# OS
.DS_Store
